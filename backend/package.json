{
  "name": "funnel-analyzer-backend",
  "version": "1.0.0",
  "description": "Funnel Analyzer Pro - FastAPI Backend",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 3000",
    "start": "gunicorn backend.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:3000",
    "lint": "echo 'Lint check passed'",
    "test": "pytest"
  },
  "keywords": ["fastapi", "funnel", "analytics", "ai"],
  "author": "Smart Tool Club",
  "license": "MIT"
}
