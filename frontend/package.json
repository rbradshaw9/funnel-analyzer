{
  "name": "funnel-analyzer-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "http-server ./public -p 4173 -c-1",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "prepare": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "http-server": "^14.1.1"
  }
}
